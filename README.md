# unified-streaming-lakehouse-platform
A distributed data system that ingests operational events in real time, processes them through a stream engine, lands them into a lakehouse on object storage, and exposes both low-latency operational metrics and interactive analytical SQL.
